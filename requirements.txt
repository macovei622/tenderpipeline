aiogram==3.13.1
google-generativeai==0.8.3
aiohttp==3.10.11
PyMuPDF>=1.25.3
aiosqlite==0.20.0
python-dotenv==1.0.1
pydantic==2.9.2
loguru==0.7.2
openai>=1.40.0

# ── OCR та парсинг документів (v2.1) ──────────────────────────────────────────
pdfplumber>=0.11.0       # Витягування таблиць зі структурних PDF
pdf2image>=1.17.0        # Конвертація сторінок PDF в зображення для OCR
pytesseract>=0.3.13      # Python-обгортка Tesseract OCR
Pillow>=10.0.0           # Обробка зображень
openpyxl>=3.1.2          # Парсинг кошторисів у Excel з АВК-5

# ── Регресійне тестування ──────────────────────────────────────────────────────
pytest>=8.0.0            # Тест-раннер
pytest-asyncio>=0.23.0   # Підтримка asyncio у pytest

# ── Аналітичні модулі (analytics/ пакет) ──────────────────────────────────────
networkx>=3.3            # Модуль 2б: CPM Engine (критичний шлях)
httpx>=0.27.0            # Модуль 1, 3, 6: async HTTP клієнт
beautifulsoup4>=4.12.0   # Модуль 2а: HTML парсинг court.gov.ua
lxml>=5.0.0              # Швидший парсер для BeautifulSoup
